const SingleItem = (props) => {
  const {
    name,
    description,
    image,
    price,
    company,
    category,
    shipping
  } = props;
  return <>{name}</>;
};

export default SingleItem;
